{
  "name": "@eidolon/desktop",
  "productName": "Eidolon",
  "version": "0.1.0",
  "private": true,
  "description": "Native macOS shell for Eidolon.",
  "main": "src/main.cjs",
  "scripts": {
    "dev": "EIDOLON_DESKTOP_APP_URL=${EIDOLON_DESKTOP_APP_URL:-http://localhost:3100} npm_config_node_linker=hoisted electron-forge start",
    "package": "node scripts/package-app.mjs",
    "make": "node scripts/rebuild-dmg-native.cjs && node scripts/package-app.mjs && npm_config_node_linker=hoisted electron-forge make --skip-package",
    "make:mac": "node scripts/rebuild-dmg-native.cjs && node scripts/package-app.mjs && npm_config_node_linker=hoisted electron-forge make --skip-package --platform=darwin"
  },
  "devDependencies": {
    "@electron-forge/cli": "^7.11.2",
    "@electron-forge/maker-dmg": "^7.11.2",
    "@electron/packager": "^20.3.0",
    "electron": "^43.4.0",
    "fs-xattr": "0.3.1",
    "macos-alias": "0.2.12",
    "node-gyp": "^12.3.0"
  }
}
